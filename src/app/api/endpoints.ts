export const API_BASE_URL = 'http://localhost:5010/core/api';

export const ENDPOINTS = {
  // login endpoint
  LOGIN: `${API_BASE_URL}/login`,
  //compony endpoints
  company: `${API_BASE_URL}/company`,
  // user endpoints
  user: `${API_BASE_URL}/user`,
};

